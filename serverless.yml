service: serverlesswp-wordpress-starter

provider:
  name: aws
  runtime: nodejs22.x
  httpApi:
    payload: '2.0'

functions:
  requestHandler:
    handler: api/index.handler
    memorySize: 1024
    timeout: 30
    events:
      - httpApi:
          method: any
          path: /{proxy+}
    environment:
      #DATABASE: 'm5285_axcs'
      #USERNAME: 'm5285_noux'
      #PASSWORD: '@B888dmf70..'
      #HOST: 'mysql0.serv00.com'
